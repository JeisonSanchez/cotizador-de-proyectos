var requireDir 	= require('require-dir'),
	tasks		= requireDir('./gulp/tasks', {recurse: true});

/*
	Comando para instalar dependencia:
	Normal    : npm install --save-dev [dependencia]
	Abreviado : npm i -D [dependencia]
*/
